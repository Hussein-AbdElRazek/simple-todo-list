import { Typography } from "@mui/material";

const Empty = () => {
    return (
        <Typography
            variant="caption"
            sx={{
                textAlign: "center",
                width: "100%",
                display: "block",
            }}
        >
            Empty
        </Typography>
    );
};

export default Empty;
